{
    "globalHeaders": {
      "Access-Control-Allow-Origin": "*",
      "Access-Control-Allow-Methods": "POST, GET, OPTIONS"
    },
    "routes": [
        {
            "route": "/api/getSpeechToken",
            "methods": ["GET", "POST"]
        }
    ]
}
